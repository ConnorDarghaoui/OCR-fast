name: Release Please

on:
  push:
    branches:
      - main

permissions: {}

concurrency:
  group: release-please-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - id: release
        uses: googleapis/release-please-action@c2a5a2bd6a758a0937f1ddb1e8950609867ed15c
        with:
          release-type: rust
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  build-release:
    name: Release — ${{ matrix.name }}
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    needs: release-please
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            bin: ocrfast
            lib: libpdfium.so
            asset: ocrfast-linux-x86_64
            ext: tar.gz

          - name: Windows x86_64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            bin: ocrfast.exe
            lib: pdfium.dll
            asset: ocrfast-windows-x86_64
            ext: zip

          - name: macOS ARM64
            os: macos-latest
            target: aarch64-apple-darwin
            bin: ocrfast
            lib: libpdfium.dylib
            asset: ocrfast-macos-arm64
            ext: tar.gz

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Dependencias del sistema (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y pkg-config libssl-dev

      - name: Instalar Rust toolchain estable
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Cache dependencias Cargo
        uses: Swatinem/rust-cache@f13886b937689c021905a6b90929199931d60db1
        with:
          shared-key: release-${{ matrix.target }}
          cache-directories: |
            target/pdfium-download

      - name: Build release
        run: cargo build --release --verbose

      - name: Empaquetar (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp target/release/${{ matrix.bin }} dist/
          [ -f "${{ matrix.lib }}" ] && cp "${{ matrix.lib }}" dist/ || true
          tar -czf "${{ matrix.asset }}.tar.gz" -C dist .

      - name: Empaquetar (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item "target\release\${{ matrix.bin }}" dist\
          if (Test-Path "${{ matrix.lib }}") {
            Copy-Item "${{ matrix.lib }}" dist\
          }
          Compress-Archive -Path dist\* -DestinationPath "${{ matrix.asset }}.zip"

      - name: Subir artefacto
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: ${{ matrix.asset }}
          path: ${{ matrix.asset }}.${{ matrix.ext }}
          if-no-files-found: error

  publish-release-assets:
    name: Adjuntar binarios al release
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    needs:
      - release-please
      - build-release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Descargar artefactos
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093

      - name: Adjuntar artefactos a la release creada por Release Please
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.release-please.outputs.tag_name }}" \
            ocrfast-linux-x86_64/ocrfast-linux-x86_64.tar.gz \
            ocrfast-windows-x86_64/ocrfast-windows-x86_64.zip \
            ocrfast-macos-arm64/ocrfast-macos-arm64.tar.gz \
            --clobber
